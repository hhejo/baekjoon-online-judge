# 10162 전자레인지



```python
T = int(input())
if T % 10:
    print(-1)
else:
    A = T // 300  # 5분으로 나눔
    B = (T % 300) // 60  # 5분으로 나눈 나머지를 1분으로 나눔
    C = ((T % 300) % 60) // 10  # 위에서 다 하고 남은 나머지를 10초로 나눔
    print(A, B, C)
```

