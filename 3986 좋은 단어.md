# 3986 좋은 단어



```python
li = [input() for _ in range(int(input()))]
cnt = 0
for word in li:
    stk = []
    for ch in word:
        if stk and stk[-1] == ch:
            stk.pop()
        else:
            stk.append(ch)
    if not stk:
        cnt += 1
print(cnt)
```

