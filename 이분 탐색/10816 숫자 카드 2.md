# 10816 숫자 카드 2



```python
N = int(input())
A = sorted(map(int, input().split()))
M = int(input())
B = list(map(int, input().split()))
h = {}
for a in A:
    if a in h:
        h[a] += 1
    else:
        h[a] = 1
ans = [h[b] if b in h else 0 for b in B]
print(*ans)
```

